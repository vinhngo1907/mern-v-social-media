'use strict';

const logger = require("node-color-log");

class CommentSocketContoller {
    createComment(io, socket, users, comment){

    }

    likeComment(io, socket, users, comment){
        
    }
}

module.exports = new CommentSocketContoller;