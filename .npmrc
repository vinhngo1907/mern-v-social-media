# .npmrc
# Hoisting is often needed for legacy packages to find their dependencies
shamefully-hoist=true

# Keep workspace-specific configurations clean
link-workspace-packages=true
prefer-workspace-packages=true